word ::= ? .+ ?

rule ::= <nq_term>+ '=' <term>* '.'
query ::= <term>+ '?'

nq_term ::= '+' | '-' | '<' | '>' | ',' | '~' | <word>
term ::= <nq_term> | '(' <term>* ')'

program ::= ( <rule> | <query> )*
